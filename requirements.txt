# Core (always required)
send2trash
cryptography

# Required only if KEY_SOURCE=dpapi (Windows DPAPI-protected key file)
pywin32; sys_platform == "win32"

# Required only if BACKUP_DESTINATION_TYPE=s3
boto3

# Required only if BACKUP_DESTINATION_TYPE=azure
azure-storage-blob
